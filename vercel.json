{
  "buildCommand": "pnpm install && pnpm run build",
  "installCommand": "pnpm install --no-frozen-lockfile || (pnpm install && echo '⚠️ Lockfile atualizado durante deploy')",
  "devCommand": "pnpm dev",
  "rootDirectory": "apps/site"
}
